# zse-singlevehicle
A FiveM Enhanced resource for streaming a singular vehicle!

## How to use
Place your `.yft`, `.ytd` files into `stream_enhanced` and delete the `.gitkeep` file.

Then edit the datafile and replace `MODELNAME` to the name of your main `.yft` file.
